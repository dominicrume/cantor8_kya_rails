# Generated by `auditor live` from the brief you typed.
# This is yours to edit — it is the definition of 'what was asked for',
# and every scope-drift finding is measured against it.
name: kya_rails
version: 1.0.0
_derived_from_brief: true
features:
- id: scaffold.daml.project
  description: Scaffold the Daml project so KyaMandate.daml builds with daml build
- id: test.suite.where
  description: Test suite where every test is named after the attack it proves
- id: enforce.spend.cap
  description: Enforce spend cap, counterparty allow-list, expiry and revoke inside
    the Daml choice body
- id: chargerecord.contract.created
  description: ChargeRecord contract created on-ledger in the Charge choice, readable
    by owner and payee
- id: receipt.chain.one
  description: 'Receipt chain: one sealed receipt per attempt, accepted or refused,
    sha256 chained to the previous seal'
- id: canonicalisation.byte.identical
  description: Canonicalisation byte-identical in Python and JS, ASCII-only in hashed
    fields
- id: currency.code.instrument
  description: Currency code and instrument in the receipt, currency symbols rendered
    at display time only
- id: offline.verifier.page
  description: Offline verifier page that recomputes every seal and cascades red on
    tamper
- id: relabel.demo.story
  description: "Relabel the demo story to the OTC desk \u2014 parties, chat script\
    \ and architecture note only"
- id: whatsapp.webhook.adapter
  description: Meta WhatsApp Cloud API webhook adapter, signature-checked over the
    raw body, replay-bounded, wired into the operator server only when fully configured
- id: webhook.refusal.mutation
  description: Every refusal in the webhook adapters mutation-tested, and a complexity
    ceiling enforced with written reasons for exceptions
- id: desk.state.persistence
  description: Desk state persisted so a quote outlives the process that issued it,
    in an append-only seal-chained journal whose history cannot be edited undetected
- id: operator.recovery.view
  description: The operator screen shows each deal's age and remaining quote time on
    the desk's clock, and marks a quote that expired while the desk was off
- id: expiry.attack.attempt
  description: Expiry attack attempt recorded as a receipt
governance:
- rule: no_secrets_in_source
  description: No credentials, keys or tokens committed to the repo.
allowlist:
  external_apis: []
